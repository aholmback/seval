---
- include_role:
    name: docker

- name: spin up postgres container
  docker_container:
    name: "{{ name }}"
    state: started
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      "{{ data }}:/var/lib/postgresql/data"
